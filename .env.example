# Dev: uses Vite proxy /api -> http://localhost:8000
VITE_API_BASE_URL=/api

# Production (must match your Render backend URL, no trailing slash):
# VITE_API_BASE_URL=https://inboxpilot-backend-0dxx.onrender.com
